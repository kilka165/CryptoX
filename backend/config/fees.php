<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Ставки комиссий (доля, не проценты)
    |--------------------------------------------------------------------------
    |
    | 0.01 = 1%. Единый источник правды для расчёта комиссий на бэкенде и для
    | публичного эндпоинта GET /fees, который читает фронтенд.
    |
    */

    'trade'    => (float) env('FEE_TRADE_PERCENT', 0.01),
    'withdraw' => (float) env('FEE_WITHDRAW_PERCENT', 0.01),

];
